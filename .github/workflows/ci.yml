name: Testing
on: push

jobs:
  test_project:
    runs-on: ubuntu-latest
    steps:
    - uses: action/checkout@v2
    - name: Run Django unit tests
      run:
        pip3 install --user django
        py manage.py test